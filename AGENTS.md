# AGENTS.md

This repository's authoritative agent-context files are [`CLAUDE.md`](./CLAUDE.md) (short, auto-loaded by Claude Code) and [`docs/ARCHITECTURE.md`](./docs/ARCHITECTURE.md) (the full architecture, code-pattern walkthrough, and decision log). If your tool reads `AGENTS.md` by convention, start with `CLAUDE.md`'s "Critical gotchas" section, then read `docs/ARCHITECTURE.md` before making a structural change. This file is intentionally just a pointer so the real content isn't duplicated and doesn't drift out of sync.
